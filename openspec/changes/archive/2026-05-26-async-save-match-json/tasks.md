## 1. Backend worker (`app/jobs.py`)

- [x] 1.1 Add `_save_match_worker(file_id: str, dst: str) -> dict` that re-imports `FILE_STORE`, `LIBRARIES`, `_resolve_file`/`_shapes_for` equivalents, `find_matches_from_pointsets`, `split_matches_by_side`, `arbitrate`, and the `CLASS_*` constants inside the function body (so it pickles cleanly into the subprocess). The body SHALL be a verbatim port of the loop currently in `app/main.py:save_match_json` from the line after `_resolve_file` through the `json.dump` write — same `out` shape, same `total_matches`/`side_counts`/`arbitration_counts` accumulation, same `arbitrate(...)` call, same `dst.parent.mkdir(parents=True, exist_ok=True)` + `json.dump(out, f, indent=2)`. Return the same dict shape the handler returns today (`file_id`, `library_id`, `template_keys`, `total_matches`, `side_counts`, `arbitration_counts`, `saved_to`, `match_saved=True`).
- [x] 1.2 Add `submit_save_match(file_id: str) -> str` modelled on `submit_rule_check` (`app/jobs.py:561`): generate `job_id`, register `{"id", "file_id", "kind": "save_match", "status": "queued", "submitted_at", "started_at": None, "completed_at": None, "error": None}` under `_lock`, submit `_save_match_worker(file_id, str(match_path(file_id)))` to `_get_executor()`, attach `_on_save_match_done` as the done-callback, flip status to `running` + set `started_at`, return `job_id`.
- [x] 1.3 Add `_on_save_match_done(job_id, fut)` modelled on `_on_rule_check_done` (`app/jobs.py:598`): on `fut.result()` success store `result`, set `status=done` and `completed_at`, then `FILE_STORE.set_match_saved(file_id, True)` (import inside the callback to avoid circular import, same idiom `_on_discover_done` uses for `FILE_STORE`); on exception store the formatted traceback under `job.error`, set `status=error` + `completed_at`, and leave `match_saved` untouched.
- [x] 1.4 Add `match_path` to the import block at the top of `app/jobs.py` (it lives in `app/storage.py` alongside `rule_check_path` which is already imported).

## 2. Endpoint changes (`app/main.py`)

- [x] 2.1 Rewrite `save_match_json` (`app/main.py:1125`): keep `_resolve_file`, the `LIBRARIES.get(rec.library_id)` lookup (raise 404 if missing), and a `parsed_path(file_id).exists()` check (raise 500 if missing) as fast pre-flight validation in the handler. Then call `jobs.submit_save_match(file_id)` and return `JSONResponse(status_code=202, content={"job_id": job_id, "file_id": file_id})`. Remove all of the per-class loop, `arbitrate(...)` call, `json.dump`, and the in-handler `FILE_STORE.set_match_saved` — that logic now lives only in the worker / callback.
- [x] 2.2 Verify `GET /api/files/{file_id}/match-json` (`app/main.py:1211`) and `GET /api/jobs/{job_id}` (`app/main.py:617`) are unchanged and serve the persisted file + job dict respectively.

## 3. Frontend (`app/static/canvas.js`)

- [x] 3.1 Add a module-scoped `let saveMatchInFlight = null;` near the other viewer-level state (alongside the `$saveMatchBtn` constant at line ~35 or wherever module state for save-match lives). Set to the active `job_id` while polling, `null` otherwise.
- [x] 3.2 Rewrite `saveMatchJson()` (`app/static/canvas.js:2570`): guard with `if (saveMatchInFlight) return;`, fire the POST, on `202` capture `job_id`, set `saveMatchInFlight = job_id`, set `$saveMatchBtn.disabled = true`, set the status line to a saving-in-progress message, then call a new `pollSaveMatchJob(job_id)` helper.
- [x] 3.3 Implement `pollSaveMatchJob(jobId)`: `setInterval` (or `setTimeout` recursion) at 500 ms cadence fetching `/api/jobs/${jobId}`. On `done` use `job.result.template_keys.length`, `job.result.total_matches`, `job.result.saved_to` to render the success status (mirroring the current synchronous success message), set `currentFileInfo.match_saved = true`, call `refreshRoleSwitcher()`, clear the interval, set `saveMatchInFlight = null`, re-enable the button. On `error` surface `job.error` in the status line, clear the interval, set `saveMatchInFlight = null`, re-enable the button. On transient fetch failure (`!res.ok` or thrown) log and continue — do NOT clear the interval.

## 4. Tests

- [x] 4.1 Refactor `tests/test_match_json_constraints.py::test_save_match_json_emits_arbitration_counts` (`tests/test_match_json_constraints.py:238`) to drive the new flow: either (a) call `_save_match_worker` directly as a pure function and assert on its return value + on-disk JSON, or (b) submit via `jobs.submit_save_match` and `fut.result()` synchronously inside the test. Whichever path: the existing assertions on `arbitration_counts` / `total_matches` / `side_counts` / `template_keys` SHALL remain — these are now read from the worker return value rather than the HTTP body.
- [x] 4.2 Add a job-lifecycle test asserting: POST returns 202 with a `job_id`, the in-memory job dict carries a `save_match` entry, after the worker completes `_jobs[job_id]["status"] == "done"`, `_jobs[job_id]["result"]` has the documented shape, `FILE_STORE.get(file_id).match_saved is True`, and `data/match/{file_id}.json` exists.
- [x] 4.3 Add a worker-error test asserting: if `_save_match_worker` raises (e.g. parsed file deleted between pre-flight and worker start), the job's `status` is `error`, `job.error` is a non-empty string, `FILE_STORE.get(file_id).match_saved` remains `False`, and a follow-up `POST /api/products/{pid}/rule-check` for any product binding this file still returns the 400 "still need Save Match" rejection.
- [x] 4.4 Add a pre-flight test asserting: `POST /api/files/{file_id}/match-json` with a missing parsed file returns a synchronous 4xx/5xx (not 202) and no job is registered in `_jobs`.

## 5. Manual verification

- [x] 5.1 Launch the app, open a product with a non-trivial library, click Save Match, and confirm: button disables, viewer remains responsive (pan / zoom / frame-select / role-tab switch all work) while the status shows the in-progress message, button re-enables on completion with the success summary, role switcher's green readiness dot updates.
- [x] 5.2 Double-click Save Match in quick succession and confirm only one network POST fires.
- [x] 5.3 Force a worker error (e.g. temporarily move `parsed/{file_id}.json` after the pre-flight check fires) and confirm the status surfaces the error, the button re-enables, and the file's Save Match indicator stays "not saved".
